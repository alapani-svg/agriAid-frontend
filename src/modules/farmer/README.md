# Module: farmer

Farmer profile, estates, CIG affiliation, and credibility surface.

- **Live related work:** `modules/operations` (harvest / stock).
- Place UI under `components/`, screens under `pages/`, API under `services/`.
